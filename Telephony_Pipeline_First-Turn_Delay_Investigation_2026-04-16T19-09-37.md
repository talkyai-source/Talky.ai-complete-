[ ] NAME:Current Task List DESCRIPTION:Root task for conversation __NEW_AGENT__
-[ ] NAME:Instrument baseline first-turn latency (pre-change measurement) DESCRIPTION:Add structured JSON log lines with µs-precision timestamps per call: t_answer (ChannelStateChange Up), t_ari_setup_done, t_stt_ws_ready (Deepgram handshake complete), t_stt_first_final (first Flux final transcript), t_turn_end, t_llm_first_token (already at voice_pipeline_service.py:764), t_tts_ws_ready (Cartesia handshake complete), t_tts_first_audio, t_first_rtp_tx (already at C++ session.cpp:548). Run 20 outbound calls with the test caller saying 'Hello?' within 500 ms of answering. Record P50/P95 of (t_first_rtp_tx − t_stt_first_final) — the true user-perceived first-interaction latency — and (t_stt_ws_ready − t_answer) — exposes the STT handshake race. Save results to backend/docs/debugging/2026-04-16_first_turn_baseline.md.
-[ ] NAME:P0-A: Suppress agent greeting on outbound user-first calls DESCRIPTION:Add first_speaker: 'user'|'agent' to _build_telephony_session_config in telephony_bridge.py (default 'user' for outbound campaigns). In _on_new_call (lines 268-270), only schedule _send_outbound_greeting when first_speaker == 'agent'. Relocate the barge_in_event clearing from _send_outbound_greeting:199 into _on_new_call so stale callee 'Hello?' barge-ins don't remain queued. Eliminates wrong-turn greeting work (500-1500 ms wasted) and removes audible overlap with the user's first utterance. Proof: voice_pipeline_service.py:456-457 llm_active guard stays correct when greeting is skipped. Validation: no 'Generating dynamic greeting' log line for user-first calls.
-[ ] NAME:P0-C: Persistent Cartesia WebSocket with per-sentence context_id DESCRIPTION:Refactor backend/app/infrastructure/tts/cartesia.py so CartesiaTTSProvider opens ONE WebSocket per call (not per sentence). Add connect_for_call(call_id) and disconnect_for_call(call_id). Each stream_synthesize reuses the WS and sends a fresh context_id+transcript payload. Hook connect_for_call into telephony_bridge._on_new_call so the WS handshake runs in parallel with media setup. Predicted saving: 300–600 ms first turn + 60–200 ms every subsequent sentence. Proof source: docs.cartesia.ai/api-reference/tts/websocket.
-[ ] NAME:P0-B: Prewarm Deepgram Flux WebSocket at pipeline start DESCRIPTION:Split DeepgramFluxSTTProvider.stream_transcribe into connect() + stream(audio_iter). Call connect() from voice_pipeline_service.start_pipeline IMMEDIATELY on pipeline start so the WS is ready before the user speaks (critical in user-first mode — user may speak within 200 ms of answering). Send the existing 100 ms silent primer frame inside connect(). Feed buffered caller audio frames during handshake so none are lost. Predicted saving: 300–500 ms on every first user interaction — the single biggest win for the reported 2–4 s symptom. Proof: developers.deepgram.com/docs/flux/quickstart + self-logged deepgram_flux_slow_start warning at deepgram_flux.py:346.
-[ ] NAME:P0-D: Parallelize ARI round-trips in _on_outbound_answered DESCRIPTION:In backend/app/infrastructure/telephony/asterisk_adapter.py (_on_outbound_answered, lines 488–549): after the externalMedia POST, issue addChannel and the two /variable GETs concurrently via asyncio.gather. Investigate merging the two /variable GETs into a single ARI bulk-variables call or caching UNICASTRTP_LOCAL_* from ChannelVarSet events on the ARI WS. Predicted saving: 100–400 ms. Also apply the same parallelization to _on_stasis_start (inbound) for consistency.
-[-] NAME:P0-4: Remove 50 ms asyncio.sleep from _send_outbound_greeting DESCRIPTION:SUBSUMED by P0-A (greeting suppression). The 0.05 s sleep only lives inside _send_outbound_greeting; removing the greeting removes the sleep. No separate action needed.
-[ ] NAME:Post-P0 measurement gate (§6.3 criteria) DESCRIPTION:Gate criteria after P0-A…P0-E are applied, ALL must pass: (1) (t_first_rtp_tx − t_stt_first_final) P50 drops by ≥750 ms vs baseline. (2) (t_stt_ws_ready − t_answer) P95 is <0 (STT WS ready before first caller audio). (3) tts_ws_handshake_ms appears exactly once per call, not once per sentence. (4) No 'Generating dynamic greeting' log for user-first calls. Each criterion maps to exactly one P0 item. If any criterion fails, DO NOT proceed to P1 — re-examine the corresponding fix's individual log. ±30% tolerance on predicted savings.
-[ ] NAME:P1-1: Enable HTTP/1.1 keep-alive on C++ gateway HTTP server DESCRIPTION:In services/voice-gateway-cpp/src/http_server.cpp: (1) Drop 'Connection: close' in write_response (line 145); add 'Connection: keep-alive'. (2) In handle_client, loop reading additional requests on the same fd until idle timeout (5 s) or client closes. Verify aiohttp on the Python side amortizes TCP connects across TTS POSTs. Predicted saving: 10–40 ms per utterance, plus eliminates TIME_WAIT accumulation.
-[ ] NAME:P1-2: Batch TTS POSTs to C++ gateway (20 ms → 50–100 ms) DESCRIPTION:In backend/app/infrastructure/telephony/telephony_media_gateway.py send_audio: buffer 320–800 bytes before POSTing instead of 160. C++ enqueue_tts_ulaw already accepts any multiple of 160 (session.cpp:236). Verify barge-in latency stays ≤50 ms via the existing barge_in_event check. Predicted saving: cuts POST rate 50/s → 10–20/s, ~20–60 ms per 3 s utterance and large CPU win.
-[ ] NAME:P1-3: Keep-alive the C++ → Python audio callback path DESCRIPTION:In services/voice-gateway-cpp/src/http_server.cpp http_post (line 439): replace HTTP/1.0+Connection:close with HTTP/1.1 keep-alive and a per-url connection pool, OR switch to libcurl-easy with CURLOPT_TCP_KEEPALIVE. Alternative: use a Unix domain socket to localhost backend. Eliminates thread-per-POST storm (line 793) at scale. Not a first-turn win but a scalability fix.
-[ ] NAME:P0-E: Warm Groq httpx/HTTP-2 connection at worker boot DESCRIPTION:Promoted from P2 to P0 because in user-first mode the first real LLM call IS the first agent response — cold httpx connect of 80–200 ms sits directly on the critical path. In backend/app/workers/voice_worker.py _initialize_providers, after GroqLLMProvider.initialize, issue a dummy chat.completions.create with max_tokens=1 to seed the httpx HTTP/2+TLS pool and DNS cache. Proof: console.groq.com/docs/production-readiness/optimizing-latency recommends persistent connections. Validation: log groq_connect_ms on first real call — should be ≤100 ms after fix.
-[ ] NAME:P2-1: Replace C++ audio-callback HTTP path with WebSocket or UDS DESCRIPTION:Architectural follow-up: move the C++ → backend audio callback off per-frame HTTP POST onto a persistent WebSocket (like the existing /ws-audio/{call_uuid} FreeSWITCH path) or a Unix domain socket. Eliminates TCP connect/TLS/thread overhead entirely. Only pursue if P1 measurements show remaining scalability problems.
-[ ] NAME:P3-1: Upsample 8 kHz telephony audio to 16 kHz before Flux DESCRIPTION:Quality-only follow-up. In backend/app/infrastructure/telephony/telephony_media_gateway.py on_audio_received, upsample pcm16 from 8 kHz to 16 kHz before enqueueing, and change _build_telephony_session_config stt_sample_rate to 16000. Improves STT accuracy per Deepgram docs, reducing perceived lag from re-asks. Does NOT affect first-turn latency.
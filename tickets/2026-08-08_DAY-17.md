# DAY 17 — Sat 2026-08-08 · BUFFER

**No tickets scheduled.**

## Priority use
1. Spillover from DOC-01 … DOC-05, DOC-08.
2. **Peer review backlog.** Ground rule 5 says every doc is reviewed before it is marked complete. In
   practice reviews queue up. This is the day to clear them — a doc that is written but unreviewed is
   ⬜, not 🟢, and letting that distinction slide is how the whole standard collapses.
3. Rest.

## Sprint 2 mid-point check
- [ ] 4 of 12 docs 🟢 (DOC-01, 02, 03, 04) and 2 more drafted (DOC-05, 08)?
- [ ] Is `docs/v2/09-known-issues.md` accumulating findings? By now it should hold at least:
      dead `post_call_analyzer`, committed C++ build artifacts, hardcoded carrier credentials in
      `setup-asterisk.sh`, decentralised config (245 env vars), the 4-class ORM beside a 70-table
      schema, and whatever TKT-001/003/009 turned up. If it is thin, findings are being lost.
- [ ] Any doc that turned out to need code changes → is that recorded as a **freeze exception** with
      the ticket it unblocks? Writing docs finds bugs; that is expected and fine. Slipping the fixes
      in unrecorded is not.

## Do not
- ❌ Start the recordings early. They need finished, reviewed docs as scripts — that is the entire
  reason Sprint 3 follows Sprint 2.
- ❌ Make code changes outside the freeze-exception process.

# DAY 02 — Fri 2026-07-24 · Sprint 1: Code stabilisation & ground truth

---

## TKT-003 · Reconcile the two contradictory production deploy paths

**Maps to:** prerequisite for original tasks 10 and 15 · **Est:** 3h · **Status:** ⬜

### Goal — done means
One deploy mechanism is designated authoritative, and the other is either fixed to match reality or
made incapable of firing. `docs/v2/00-ground-truth.md` gains a "Deployment" section stating which is
which, and why.

### Why this is here
Two mechanisms in this repo claim to deploy production, and they describe different systems:

| | `deploy_to_server.sh` | `.github/workflows/deploy.yml` |
|---|---|---|
| Model | bare git checkout at `/opt/talky` | Docker image from GHCR |
| Apply | `git pull --ff-only` + `systemctl restart` | SCP `docker-compose.yml` + `docker compose up -d` |
| Trigger | manual | **automatic `workflow_run` after CI passes on `main`** |

`HANDOFF-NEXT-AGENT.md:25` states production is systemd, not compose. If that is right, then a
workflow that fires automatically on every green `main` build is either failing silently on every
push, or pointing at a host that is not production. Both are worth knowing before writing a runbook
(DOC-10) or recording a deployment video (VID-15) — see revision **R-2**.

**Depends on:** TKT-001. **Blocks:** DOC-10, VID-15, TKT-017.

### Plan
1. Read `.github/workflows/deploy.yml` end to end. Identify `DEPLOY_HOST`, `DEPLOY_PATH` and the
   trigger conditions.
2. Determine from the **GitHub Actions run history** whether the deploy workflow has ever succeeded,
   and when it last ran. `gh run list --workflow=deploy.yml` is enough.
3. Cross-check against TKT-001: is Docker even installed on the box?
4. Decide, and write the decision down with its reasoning:
   - **If systemd is authoritative** (expected): disable the automatic `workflow_run` trigger so it
     cannot fire, or gate it behind an explicit environment approval. Leave the file with a header
     comment explaining what it was and why it is dormant. Do not delete it — it is the
     starting point for a future containerised deploy.
   - **If the compose path is live somewhere** (staging?): document what it targets and how the two
     relate.
5. Confirm whether `deploy_to_server.sh:54`'s reference to `talky-voice-gateway` is correct given
   TKT-001's answer, and fix the script if it prints the status of a unit that does not exist.

### Do
- Check the actual Actions run history. The workflow's *intent* is in the file; its *behaviour* is in
  the run log.
- Preserve the dormant path with an explanatory comment. A future engineer finding a deleted deploy
  workflow in git history learns nothing; one finding a commented dormant file learns everything.
- Treat "the automatic trigger has been silently failing for weeks" as a finding for DOC-09, not an
  embarrassment to bury.

### Don't
- ❌ Do not test either path by deploying to production today.
- ❌ Do not delete `.github/workflows/deploy.yml`.
- ❌ Do not change `.github/workflows/ci.yml` gates — they are already blocking and green (**R-13**).
- ❌ Do not touch the server. This ticket is repo-side plus reading run history.

### Checklist
- [x] `deploy.yml` triggers, host and path documented — host/path are `DEPLOY_HOST`/`DEPLOY_PATH` secrets; **values undetermined from repo**
- [x] Actions run history checked — **15 runs, all "success" in 6–18 s, every real job skipped**
- [x] Docker presence on prod confirmed from TKT-001 — **Docker IS installed and runs Postgres + Redis**
- [x] Authoritative path designated in writing with reasoning — `deploy_to_server.sh`; see ground truth § Deployment
- [x] Non-authoritative path disabled/gated + header comment added — `workflow_run` trigger removed; 30-line header added; **verified by YAML parse: only `workflow_dispatch` remains**
- [x] `deploy_to_server.sh` gateway-unit reference **confirmed correct** — the gateway *is* running, so line 54 was right all along; the real problem was the missing unit file, fixed under TKT-001
- [x] "Deployment" section added to `docs/v2/00-ground-truth.md`
- [x] Peer-reviewed **(agent-reviewed)** — see § Review below

### Test cases
| # | Check | Expected | Result |
|---|---|---|---|
| 1 | Push a trivial commit to `main` | CI runs; dormant workflow does **not** fire | 🟡 **deferred with reason** — pushing is blocked on the F-14 leak. Superseded by stronger evidence: the trigger no longer exists in the file (YAML parse shows `on:` = `workflow_dispatch` only), so it *cannot* fire rather than *happens not to*. |
| 2 | `deploy_to_server.sh` read-through | every unit it names exists per TKT-001 | 🟢 all five exist and are `active` |
| 3 | Ground-truth doc | names exactly one authoritative deploy path | 🟢 `deploy_to_server.sh` |
| 4 | Repo search for a third deploy mechanism | none found, or found and documented | 🟢 **a third was found** — `.claude/commands/deploy.md`, an independent hand-written copy of the same git+systemd steps; documented |

**3/4 pass, 1 deferred.** Test 1 is the only outstanding item and it is blocked by an incident, not by
this ticket. Recorded rather than waived: the substitute evidence is *stronger* than the original test
(absence of a trigger beats observing it not fire once), but it is not the test as written.

### Why this ticket's fix had to land before the CI fix
Red CI was the only thing preventing this workflow from deploying an untested containerised path to
production the moment CI went green. Fixing CI first would have switched it on by accident. That
ordering is now recorded in `tickets/README.md` so it survives this session.

---

## § Review — TKT-003 (agent-adversarial, 2026-07-24)

Reviewer: a fresh agent with no part in the implementation, instructed to find defects rather than
approve, working from `git diff` and static inspection. **Not** a human review — see
`tickets/README.md` § Amendment 1 for what that distinction means and what it does not cover.

### Findings and disposition

| # | Sev | Finding | Disposition |
|---|---|---|---|
| 1 | **Blocker** | Every one of TKT-001/002/003 ticked "Peer-reviewed — see § Review below" while **no `§ Review` section existed in any file.** Ticked-but-unsatisfied, cited a section that did not exist, and not labelled per the board's own Amendment 1. | **FIXED.** This section is the remedy. Checklist items relabelled `(agent-reviewed)`. TKT-001/002 boxes were **un-ticked** pending their own review, which was still in flight. |
| 2 | Should-fix | The new gateway unit's recovery ritual opened with "box rebuilt / file lost" but gave `cp → daemon-reload → restart`. `restart` does not create the `multi-user.target.wants` symlink, so the unit would not survive a reboot — the ritual did not solve the disaster its own header described. | **FIXED.** Header now separates **FRESH BOX** (`systemctl enable --now`) from **UPDATING** (`restart`). |
| 3 | Should-fix | `install-services.sh` globs `*.service` and would symlink the new unit, but its explicit `enable` list omitted it — a rebuild would leave it on disk, disabled, indistinguishable from missing. `talky.target`'s `Requires=` omits it too. | **FIXED.** `systemctl enable talky-voice-gateway.service` added, with a comment stating the rule. Also documented the `backend/deploy/systemd/` units the installer does not read (F-4). |
| 4 | Nit | Dead `workflow_run` branches remain in the job logic (`deploy.yml` env block, the `EVENT_NAME` branch, the staleness re-check, one `if:` guard). Harmless — Actions evaluates unmatched context as empty — but a reader grepping the body gets no signal at point of use. | **FIXED.** Inline pointer added at the branch. Not deleted: the header states this file is a preserved starting point for a future containerised deploy. |
| 5 | Nit | `concurrency.group`'s `|| 'staging'` fallback is now unreachable. | **ACCEPTED, not changed.** Vestigial but correct, and it becomes live again if the trigger is ever restored. Simplifying it would remove a safety default for no gain. |
| 6 | Nit | The unit omits `StartLimitIntervalSec`/`StartLimitBurst` that the three workers set; the header defended two divergences but not this third. | **FIXED.** Folded into the header's list of intentional divergences with the systemd default stated. |

### What the reviewer independently confirmed sound

- **The gating goal is met.** PyYAML parse: top-level `on:` is exactly `{workflow_dispatch: …}`. No
  `push`, `schedule` or `pull_request`; no other workflow invokes this one via `workflow_call` or
  `repository_dispatch`. It cannot fire except by manual dispatch.
- YAML still valid; all six jobs intact.
- `Type=exec` is valid systemd (≥240) and empirically proven — the captured unit has been running
  since 2026-05-12.
- `User=admins` is genuinely the only non-root service; the other four units have no `User=`.
- `ExecStart` path exists in the tree.
- **No `EnvironmentFile` is correct, not an oversight** — `http_server.cpp` fails closed only when
  binding a non-loopback host without `VOICE_GATEWAY_AUTH_TOKEN`; this binds `127.0.0.1`.
- **The chmod is correct and non-functional**, exactly as F-6 records after its self-correction.
  Nothing in the repo invokes `./healthwatch.sh` directly.

### Pre-existing issues the reviewer raised, outside this ticket's scope

- `docs/DEPLOYMENT.md` and `docs/RUNBOOK.md` still describe a live compose/GHCR flow with no pointer
  to the "dormant, never executed" finding. **Logged for DOC-10** — a reader landing there instead of
  the ground-truth doc is still misled. This is the cheapest remaining risk from this ticket's goal.
- `telephony/docs/phase_3/workflows/deploy.yml` is an archived snapshot with an even older
  `on: push` trigger. Path marks it historical; low risk.

### Limits of this review — stated, not glossed
The reviewer could not SSH, could not run `daemon-reload` against a real host, and validated YAML with
PyYAML rather than GitHub's own Actions schema validator. Syntactic validity is proven; GitHub-side
schema acceptance is not, and will only be proven when the file is next pushed.

### Deliverable
`docs/v2/00-ground-truth.md` § Deployment + the guarded workflow file

---

## TKT-004 · Live validation call + mine the unanalysed 2026-07-20 test calls

**Maps to:** validates original tasks 24, 28, 30, 32 · **Est:** 3h · **Status:** ⬜

### Goal — done means
One controlled outbound call is placed and analysed end-to-end, proving the greeting-truncation fix
works on real audio, plus a written analysis of the existing unanalysed test calls from
2026-07-20 ~11:25–11:30.

### Why this is here
Seven deploys landed over two days and **nothing shipped this week has been heard on a real call**.
The greeting fix (commit `33fee92c`) was justified by log evidence showing `interrupted=True` at ~1s
on both 07-20 test calls. That fix is unproven in production audio. Every doc and video in Sprints 2
and 3 describes a system nobody has recently listened to.

**Depends on:** TKT-001. **Blocks:** DOC-01, DOC-05, VID-14, VID-16 (which needs a real failed call
to trace).

### Plan
1. **Mine the existing calls first — it is free.** Pull logs for the 2026-07-20 11:25–11:30 window and
   analyse turn-taking, latency and greeting behaviour. This may answer the question without dialling.
2. Confirm the call setup with the user before dialling. Known-good configuration:
   - Tenant `info@allstateestimation.co.uk` (`790ca2db-6696-4fe9-9a2c-cd690c414a1e`)
   - Destination **`+442046132300`**, trunk `blaze-pool-150004`, caller-ID **`+442046132301`**
     (an earlier `+4420461323` was one digit short — verify before dialling)
   - LLM `qwen/qwen3.6-27b`; TTS currently **ElevenLabs** — switch to Cartesia in AI Options only if
     the user still wants that trial.
3. Place **one** call. Capture the full log window.
4. Verify against the checklist below and write `docs/v2/validation-call-2026-07-24.md`.

### Do
- Analyse the free evidence before generating new evidence.
- Watch Qwen 3.6 27B closely — it was pulled once before (2026-06-27) for dodging the AI-disclosure
  question and hallucinating. If it misbehaves, that is a finding, and a model rollback is a
  legitimate outcome of this ticket.
- Keep the recording. VID-14 and VID-16 both need real call material.

### Don't
- ❌ **Never start a campaign to test.** The allstate campaign holds ~691 real leads. One call, to one
  number the user names, placed deliberately.
- ❌ Do not dial without confirming the caller-ID digit count first.
- ❌ Do not restart services mid-call — a restart drops calls in progress.
- ❌ Do not change AI Options without telling the user which setting you changed and why.

### Checklist
- [ ] 2026-07-20 11:25–11:30 calls pulled and analysed
- [ ] Call setup confirmed with the user before dialling
- [ ] Caller-ID verified as 12 digits
- [ ] One call placed; full log window captured
- [ ] Greeting: `outbound_greeting_presynth_done ... interrupted=False`
- [ ] Turn-taking: no dead air, no talk-over, no self-interrupt
- [ ] Qwen answers checked for AI-disclosure evasion and hallucination
- [ ] Recording archived for VID-14/VID-16
- [ ] `docs/v2/validation-call-2026-07-24.md` written
- [ ] Peer-reviewed

### Test cases
| # | Test | Expected | Pass |
|---|---|---|---|
| 1 | Greeting playback | `interrupted=False` — **the fix's whole purpose** | ⬜ |
| 2 | Time to first agent audio after answer | within the established budget; recorded either way | ⬜ |
| 3 | Caller barge-in mid-greeting | agent **does** yield — immunity must not have become deafness | ⬜ |
| 4 | Post-hangup | `calls` row status `ENDED`; transcript persisted; summary generated | ⬜ |
| 5 | `journalctl -p err` during the call | no error entries | ⬜ |
| 6 | AI-disclosure question, asked directly | answered honestly, not dodged | ⬜ |

Test 3 matters as much as test 1. The greeting fix works by making the agent ignore its own echo; if
it over-fires, the agent stops listening to the human. Both directions must be proven in one call.

### Deliverable
`docs/v2/validation-call-2026-07-24.md` + archived recording

---

## End-of-day
- [ ] Update Status in `tickets/README.md`
- [ ] Days 03–04 are **buffer**. If TKT-001/003/004 slipped, they land there. If not, rest — Sprint 1
      gets heavier from Day 05.

# DAY 11 — Sun 2026-08-02 · BUFFER

**No tickets scheduled.**

## Gate into Day 12 — the god-file splits

Days 12–13 refactor **5,230 lines of live call-path code** across three files. This is the most
invasive work in the entire 30-day plan. Before starting:

- [ ] Working tree clean, everything pushed
- [ ] Full gate green at or above the TKT-002 baseline
- [ ] Production healthy: `/healthz/deep` 200, `/healthz/workers` 3/3
- [ ] TKT-004's validation call is 🟢 — you need a known-good "this is what a working call sounds
      like" reference, because after the splits you will need it again
- [ ] Rollback commit hash written down and to hand

## Why the splits are last in Sprint 1

They invalidate line numbers, import paths and module names throughout the call path. Everything else
in Sprint 1 is easier to do *before* the code moves, and all of Sprint 2 must happen *after*. This is
the hinge of the schedule.

## If Sprint 1 is running late

The splits are the **only** Sprint 1 work that can be dropped without harming production. They are
code-quality work, not a defect fix — nothing is broken because `lifecycle.py` is 2,014 lines; it is
merely hard to work in.

If Day 12 arrives with TKT-005 … TKT-011 unfinished, **finish those and drop the splits** to a v2
backlog item recorded in DOC-09 and REV-40. Shipping an unfinished refactor of the call path is
strictly worse than shipping no refactor. Make that call deliberately, and write it down.

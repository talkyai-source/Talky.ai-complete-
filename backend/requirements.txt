# ========================================
# Talky.ai Backend - Python Dependencies
# ========================================

# --- Core Framework ---
# CVE-forced ahead of the prod venv (starlette<1.3.1 / python-multipart<0.0.31
# carry known advisories; fastapi 0.139 is the matching starlette-1.3 release).
# The prod venv still runs 0.109/0.35 — upgrade it as its own controlled step.
fastapi==0.139.0
uvicorn[standard]==0.27.0
starlette==1.3.1
python-multipart==0.0.32
slowapi>=0.1.9

# --- Configuration & Settings ---
pydantic==2.12.5
pydantic-settings==2.1.0
python-dotenv==1.2.2
PyYAML==6.0.1

# --- Database ---
sqlalchemy==2.0.25
psycopg2-binary==2.9.11
asyncpg==0.29.0

# --- Database Migrations (NEW) ---
alembic==1.13.1

# --- Cache & Queue ---
redis==5.0.1
hiredis==2.3.2

# --- HTTP Clients ---
httpx==0.28.1
# CVE-forced ahead of the prod venv: aiohttp<3.14.1 carries known advisories.
aiohttp==3.14.1
aiofiles==23.2.1

# --- Email (SMTP) ---
aiosmtplib>=2.0.0

# --- WebSocket ---
websockets==13.1

# --- AI Providers: STT ---
deepgram-sdk==5.3.0

# --- AI Providers: LLM ---
groq==0.37.0
# Google Gemini / Gemma — modern unified SDK. NOT the deprecated
# `google-generativeai` package. Used by GeminiLLMProvider.
google-genai==2.0.1

# --- AI Providers: TTS ---
cartesia==2.0.17
google-cloud-texttospeech>=2.16.0

# --- Telephony ---
vonage>=3.0.0
phonenumbers>=8.13.0

# --- Authentication & Security ---
PyJWT>=2.8.0
email-validator>=2.0.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
# CVE-forced ahead of the prod venv: <48.0.1 carries GHSA-537c-gmf6-5ccf.
cryptography==48.0.1
argon2-cffi==23.1.0
pyotp==2.9.0
qrcode==8.0
Pillow>=10.0.0
webauthn==2.7.1

# --- AI Assistant Agent ---
# Pinned to the production-verified stack (live server venv, 2026-07-16).
# The previous floating ranges (>=) let local, prod, and clean installs
# resolve to three different — partly import-incompatible — version sets.
langchain-core==1.3.3
langgraph==1.1.10
langgraph-prebuilt==1.0.13
langchain-groq==1.1.2
langsmith==0.8.3

# --- Template Engine ---
Jinja2>=3.1.0

# --- Audio Processing ---
numpy>=1.26.0
pydub==0.25.1
# soxr: band-limited-sinc resampler for the telephony 16<->8 kHz path
# (resample_audio in app/utils/audio_utils.py). REQUIRED — without it every
# outbound TTS chunk fails to encode and the agent is silent on calls.
soxr>=0.3.7

# --- Object Storage — S3 / R2 (NEW) ---
boto3>=1.34.0
botocore>=1.34.0

# --- OpenTelemetry — Distributed Tracing (NEW) ---
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-asyncpg>=0.45b0
opentelemetry-instrumentation-redis>=0.45b0
# OTLP exporter — sends traces to Grafana Tempo, Jaeger, etc.
# Remove if you only want console output in dev.
opentelemetry-exporter-otlp-proto-grpc>=1.24.0

# --- Monitoring & Utilities ---
prometheus-client==0.19.0
psutil>=5.9.0
pytz==2025.2
# IANA zone database for zoneinfo (time-of-day greeting). Linux hosts have
# /usr/share/zoneinfo, but Windows and slim containers do NOT — without this,
# local_hour() silently returns None and greetings lose time-of-day.
tzdata>=2025.2

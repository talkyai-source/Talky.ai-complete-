# Kamailio Modules Backup (Non-Active)

This directory is intentionally kept as a placeholder backup path.

- Active SIP-edge module work must go in `telephony/modules/opensips/`.
- Keep Kamailio-specific code here only if a formal rollback plan requires it.
